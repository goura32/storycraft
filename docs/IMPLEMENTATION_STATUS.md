# Storycraft V1 実装状況

> この文書は確認時点の実装・検証記録であり、仕様正本ではありません。現在の契約は[仕様書](SPECIFICATION.md)に従います。

## 現在の仕様との差分（2026-07-28）

この節は、仕様書と現行コードを読み合わせた記録です。以下はコード・試験・テスト用資料を変更せずに確認した差分であり、仕様を弱めるものではありません。

- 計画版の切替と、現行版だけを完結判定する保存・検証は未実装です。
- 注意付き完結の公開注意種別と、公開原稿冒頭への定型文出力は未実装です。
- 固定5回のシード変更再呼出し、修正上限時の注意付き採用、品質上限で停止しない遷移は未実装です。現行コードは工程別設定の再試行を使い、品質上限では停止します。
- 指摘対象だけに修正範囲を制限せず、成果物全体の整合性・品質改善のために置き換える契約は未実装です。現行コードは修正範囲を検証して制限します。
- 完結判定の準備中に正本・参照の不整合を検出した場合、解決記録を保存して人手確認待ちへ移す処理は未実装です。
- 完結判定中の予算待ちを `blocked` と停止理由 `budget_exhausted` で保存し、予算追加後に同じ工程から再開する処理は未実装です。
- 公開確定の中断を表すテスト用資料は、`current_stage` が現行コードの要求する `publication` ではないため、関連試験が失敗します。
- 巻の引継ぎ後に巻単位で公開判定・公開原稿作成を行う処理は未実装です。現行コードの公開は、全巻のシリーズ完結判定後だけです。

これらは実装修正が必要な差分です。現行コードの公開判断には、仕様書だけでなく、現在の自動試験と配布物の動作確認を用います。

過去の自動試験・配布物確認日: 2026-07-26
過去の確認時のブランチ名: `audit/v1-only-contracts`

この記録には当時のコミット識別子と実行コマンドが残っていないため、以下の件数は現在の状態を示すものでも、再現可能な検証証跡でもありません。

## 確認済みの範囲

- 依頼文またはキーワードの入力から公開用 Markdown 原稿までの V1 の流れ
- 新規実行 `run`、再開 `resume`、一工程実行 `step`、状態表示 `status`、検証 `validate`
- LLM 生成物の採用、場面の確定、公開用原稿のまとめての確定と復旧
- 作業場所の排他制御、不変の成果物、LLM 提供者に依存しない検証・復旧
- 構造化された出力、確認と修正、変更範囲の制約、呼出し記録
- 当時の自動試験 527 件と、隔離した配布物の作成・インストール確認

## 実際の LLM による確認

最初の作品設計を対象に、大規模言語モデル（LLM）による構造化された出力、批評と改稿の繰返し、依頼文の文体指定の保持、修正範囲の制約を確認済みです。

工程ごとの確認形式を追加した後の最終的な実 LLM 再試験は、費用と実行時間を考慮して実施していません。この未実施は自動試験または配布物の動作確認の失敗を意味しません。

## 記録上の注意

この過去記録は上記日付・ブランチ時点の結果です。公開判断では、現在の仕様、現在の実装、現在の自動試験、現在の配布物の動作確認を改めて確認します。
