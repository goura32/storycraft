# 場面本文 生成

次の場面カードから場面本文を書いてください。

## 場面カード
{{ card | tojson(ensure_ascii=False, indent=2) }}

## 文脈
視点人物の知識・読者の知識・許可された台帳情報・時刻・場所・人物情報・前場面の引継ぎ要約
{{ context | tojson(ensure_ascii=False, indent=2) }}

## 指示
**ID命名ルール（厳守）**
- thread_updates[].id: 既存の `thread-XX` のみ
- thread_updates[].action: `導入`|`進展`|`回収`|`展開` のみ
- character_updates[].character_id: 既存の `char-XX` のみ
- relationship_updates[].relationship_id: 既存の `rel-XX` のみ
- entity_updates[].entity_id: 既存の `entity-XX` のみ
- timeline_updates[].timeline_id: 既存の `time-XX` のみ
- **新規IDを勝手に作らない・日本語・記号混在のIDは不可**

本文は1,800〜2,600字程度で書いてください。
視点人物以外の内心や秘密は、その人物の知識範囲を超えて書かないでください。
情報は行動・対立・会話・観察を通じて示し、地の文だけで説明し続けないでください。

※ 上記のIDは例示です。必ず文脈で許可された実在IDのみを使用してください。

## 出力スキーマ
{{ schema }}

必ずJSONオブジェクトだけを返してください。