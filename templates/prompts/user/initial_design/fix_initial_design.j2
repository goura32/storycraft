# initial_design の修正

## 実行指示

指摘を踏まえ、Initial Design Candidate 全体を修正する。元候補 ID と確認記録 ID は、システム側の候補記録だけが持つ。

## 必須条件

- 依頼のpremise、required_elements、genreを根拠にする
- 依頼のvolume_countで継続可能な構造にする
- 依頼のending_preferenceと矛盾しない
- 依頼のavoidに該当する内容を導入しない
- 一巻だけで完結する仕組みにしない
- 未入力の固有名詞、制度、診断名、場所、事件の具体的事実を補わない
- 自然な日本語で書く
- 各fieldの役割を重複させない

## 入力

### 採用済み依頼と設定

{{ context | tojson }}

### 現在の Candidate 全体

{{ candidate | tojson }}

### 有効な確認記録 全体

{{ critique | tojson }}

## 出力スキーマ

{{ output_schema }}