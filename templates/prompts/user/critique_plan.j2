# 全巻計画の批評

候補を入力企画と照合し、修正可能な契約違反だけを抽出する。巻数、指定章数、巻・章番号の連番、非空の題・変化、最終巻以外の未解決問い、最終巻での結末先延ばしを確認する。自然な日本語以外、簡体字・中国語混入、明白な誤字も確認する。

## 出力スキーマ

```json
{"issues":[{"severity":"critical | major | minor","field":"問題のJSONパス","description":"確認できる具体的な問題","suggestion":"直接適用できる修正指示"}]}
```

問題がなければ `issues` は空配列にする。称賛、点数、出版可否、曖昧な感想、候補に存在しない問題は出さない。各issueは4キーを必ず持つ。

## 候補

{{ candidate | tojson(ensure_ascii=False, indent=2) }}

## 入力状態

{{ context | tojson(ensure_ascii=False, indent=2) }}

JSONオブジェクトだけを返すこと。
