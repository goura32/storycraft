# 巻の章一覧 生成

次の巻計画から、章一覧を作ってください。

## 巻計画
{{ vol_plan | tojson(ensure_ascii=False, indent=2) }}

## 企画
{{ brief | tojson(ensure_ascii=False, indent=2) }}

## これまでの巻の要約
{{ prior_summaries | tojson(ensure_ascii=False, indent=2) }}

## 未回収台帳
{{ threads | tojson(ensure_ascii=False, indent=2) }}

{% if is_final %}
## 注意
最終巻です。台帳で未回収の項目を、まだ書いていない章へ回収先として配置してください。
{% endif %}

## 指示
**ID命名ルール（厳守）**
- chapters[].id: `ch-XX` 形式（2桁数字）
- **新規IDを勝手に作らない・日本語・記号混在のIDは不可**

各章は章の役割、開始状態、終了状態、場面数（2〜4）だけを含める。本文や場面の詳細は含めない。

## 出力スキーマ
{{ schema }}

必ずJSONオブジェクトだけを返してください。