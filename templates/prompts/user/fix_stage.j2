# {{ stage }} の修正

批評issuesに従い候補を修正する。対象工程の出力契約を満たし、指摘されていない正しい事実、ID、順序、本文内容を失わない。工程の所有範囲外の情報を変更・追加しない。

{% if stage == "plan" %}
全巻構成の修正では、問題になった巻だけでなく、全巻を入力briefに再照合する。briefにない固有名・出来事・因果・設定を追加しない。調査方法、資料内容、制度、人物関係、出来事の原因を補完しない。各断定文がbriefのどの記述に根拠を持つか確認し、根拠を指せない名詞句・出来事・因果は削除して未確定の高水準の表現に戻す。`change` は240文字以内、`leaves_question` は160文字以内、`ending_condition` は240文字以内に縮める。最終巻以外の `ending_condition` は正確に `未設定` とし、最終巻はbrief.endingの到達条件を落とさない。返答前に全フィールドを読み直し、ハングルを含む日本語以外の文字列、不自然な助詞や語尾、誤字、非標準字形、英語の混入を残さない。
{% endif %}

## 候補

{{ candidate | tojson }}

## 批評issues

{{ critique | tojson }}

## 入力状態

{{ context | tojson }}

## 出力スキーマ

{{ output_schema }}
