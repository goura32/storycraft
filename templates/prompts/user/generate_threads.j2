# 伏線・重要イベント台帳 生成

次の情報から、伏線・重要イベント台帳を作ってください。

## 企画
{{ brief | tojson(ensure_ascii=False, indent=2) }}

## 全巻計画
{{ series_plan | tojson(ensure_ascii=False, indent=2) }}

## 人物
{{ characters | tojson(ensure_ascii=False, indent=2) }}

## 世界台帳
{{ world | tojson(ensure_ascii=False, indent=2) }}

## 時間台帳
{{ timeline | tojson(ensure_ascii=False, indent=2) }}

## 指示
**ID命名ルール（厳守）**
- threads[].id: `thread-XX` 形式（2桁数字）
- threads[].involved_characters: 既存の `char-XX` のみ
- threads[].must_not_reveal_before / introduce_by / resolve_by: 既存の `time-XX` のみ
- **新規IDを勝手に作らない・日本語・記号混在のIDは不可**

- 入れるのは最終的な説明・発見・対決・約束の実現など回収が必要なものだけ
- 各項目に導入先（introduce_by）と回収先（resolve_by）を必ず決める
- id は仮連番（thread-01 等）、involved_characters は人物の id（char-01 等）で書く

## 出力スキーマ
{{ schema }}

必ずJSONオブジェクトだけを返してください。