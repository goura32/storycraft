# 場面カードの修正

批評issuesに従い候補を修正する。指摘されていない正しいIDと順序を失わない。`scene_id` は入力と完全一致させ、両ID配列は入力の既知IDだけにし、更新許可IDは可視IDの部分集合にする。最終場面では全未回収IDを両配列に含める。

## 候補

{{ candidate | tojson }}

## 批評issues

{{ critique | tojson }}

## 入力状態

{{ context | tojson }}


## 出力スキーマ

{{ output_schema }}
