{
  "type": "object",
  "required": ["content", "handoff_summary", "state_updates"],
  "properties": {
    "content": {"type": "string", "description": "完成した日本語小説本文"},
    "handoff_summary": {"type": "string", "description": "本文に根拠を持つ空でない引継ぎ要約"},
    "state_updates": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "status"],
        "properties": {
          "id": {"type": "string", "description": "card.allowed_update_idsにあるID"},
          "status": {"type": "string", "enum": ["open", "in_progress", "resolved"]}
        }
      }
    }
  }
}
