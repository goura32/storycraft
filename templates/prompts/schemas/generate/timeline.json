{
  "type": "object",
  "required": ["timelines"],
  "properties": {
    "timelines": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "kind", "description", "starts_at", "ends_at", "related_ids", "status", "actual_scene"],
        "properties": {
          "id": {"type": "string"},
          "kind": {"type": "string"},
          "description": {"type": "string"},
          "starts_at": {"type": "string"},
          "ends_at": {"type": "string"},
          "related_ids": {"type": "array", "items": {"type": "string"}},
          "status": {"type": "string", "enum": ["予定", "進行中", "完了", "失効"]},
          "actual_scene": {"type": ["string", "null"]}
        }
      }
    }
  }
}