{
  "type": "object",
  "required": ["chapters"],
  "properties": {
    "chapters": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["chapter_number", "title", "purpose", "start_state", "end_state", "scene_count"],
        "properties": {
          "chapter_number": {"type": "integer"},
          "title": {"type": "string"},
          "purpose": {"type": "string"},
          "start_state": {"type": "string"},
          "end_state": {"type": "string"},
          "scene_count": {"type": "integer", "minimum": 2, "maximum": 4}
        }
      }
    }
  }
}