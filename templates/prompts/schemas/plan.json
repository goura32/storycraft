{
  "type": "object",
  "required": ["volumes"],
  "properties": {
    "volumes": {
      "type": "array",
      "description": "入力briefの巻数と同じ順序で並べる全巻構成。配列の位置が巻順を表す。",
      "items": {
        "type": "object",
        "required": ["title", "change", "leaves_question"],
        "properties": {
          "title": {
            "type": "string",
            "maxLength": 48,
            "description": "この巻の中心的な変化または問いを読者向けに表す、48文字以内の自然な日本語の巻題。"
          },
          "change": {
            "type": "string",
            "maxLength": 240,
            "description": "前巻からこの巻へ進んだことで、シリーズ全体の問いに対する理解・緊張・選択の局面がどう進むかを240文字以内で書く。人物の履歴、世界設定、具体的な手順、制度の細部は書かない。"
          },
          "leaves_question": {
            "type": "string",
            "maxLength": 160,
            "description": "160文字以内。この巻の後に残る主要な未解決の問い。最終巻は空文字列にする。"
          }
        }
      }
    }
  }
}
